import neo4j from "neo4j-driver";
const driver=neo4j.driver(process.env.NEO4J_URL??"bolt://localhost:7687",neo4j.auth.basic(process.env.NEO4J_USER??"neo4j",process.env.NEO4J_PASSWORD??"aiops12345"));
const session=driver.session();
const nodes=[['api-gateway','business-service'],['order-service','business-service'],['payment-service','business-service'],['inventory-service','business-service'],['user-service','business-service'],['notification-service','business-service'],['mysql-primary','database'],['mysql-replica','database'],['redis-cache','cache'],['elasticsearch','search'],['kafka-broker','middleware']];
const edges=[['api-gateway','order-service'],['api-gateway','user-service'],['order-service','payment-service'],['order-service','inventory-service'],['order-service','notification-service'],['payment-service','mysql-primary'],['payment-service','redis-cache'],['inventory-service','mysql-primary'],['inventory-service','elasticsearch'],['user-service','mysql-primary'],['user-service','redis-cache'],['notification-service','kafka-broker'],['mysql-primary','mysql-replica'],['order-service','kafka-broker'],['api-gateway','redis-cache']];
for(const [name,type] of nodes)await session.run('MERGE (n:Node {name:$name}) SET n.type=$type',{name,type});for(const [from,to] of edges)await session.run('MATCH (a:Node {name:$from}),(b:Node {name:$to}) MERGE (a)-[:DEPENDS_ON]->(b)',{from,to});console.log(`seeded ${nodes.length} nodes / ${edges.length} dependencies`);await session.close();await driver.close();
