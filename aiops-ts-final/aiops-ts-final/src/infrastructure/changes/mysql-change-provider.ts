import mysql,{type Pool} from "mysql2/promise"; import type {ChangeProvider} from "./change-provider.js";
export class MySqlChangeProvider implements ChangeProvider { private pool:Pool; constructor(){this.pool=mysql.createPool({host:process.env.MYSQL_HOST??"localhost",port:Number(process.env.MYSQL_PORT??3306),user:process.env.MYSQL_USER??"aiops",password:process.env.MYSQL_PASSWORD??"aiops",database:process.env.MYSQL_DATABASE??"aiops"});} async query(node:string,start:string,end:string){const [r]=await this.pool.query<any[]>("SELECT change_type,version,occurred_at,summary FROM change_record WHERE node_name=? AND occurred_at BETWEEN ? AND ? ORDER BY occurred_at",[node,start,end]); return r.map(x=>({type:x.change_type,version:x.version??undefined,occurredAt:x.occurred_at,summary:x.summary}));}}
